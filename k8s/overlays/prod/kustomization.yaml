apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tas-mcp-prod

resources:
  - ../../base
  - hpa.yaml
  - vpa.yaml
  - ingress.yaml

namePrefix: prod-

commonLabels:
  environment: production

replicas:
  - name: tas-mcp
    count: 5

images:
  - name: tas-mcp
    newTag: v1.0.0